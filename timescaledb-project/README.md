# TimescaleDB Real-Time Monitoring

This project collects system metrics (CPU, memory, disk) and stores them in TimescaleDB using hypertables. It provides real-time analytics and a dashboard using Streamlit.

## Run

pip install -r requirements.txt  
python collector.py  
streamlit run dashboard.py
